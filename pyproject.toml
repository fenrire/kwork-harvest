[project]
name = "slack-harvest"
version = "0.2.0"
description = "슬랙 메시지를 노션에 채널/주제별로 정리하는 도구"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.24.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
slack-harvest = "main:main"
